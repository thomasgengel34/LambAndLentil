@using LambAndLentil.Domain.Entities
@model ContainerSizeUnit?

@{
    string actionM = Request.AppRelativeCurrentExecutionFilePath.Split('/').Skip(2).Take(1).Single();
    ContainerSizeUnit? valueM = null;
    if (ViewData["valueM"] != null)
    {
        valueM = (ContainerSizeUnit?)ViewData["valueM"];
    }
    string colon = " : ";
}

@if (actionM == UIViewType.Edit.ToString() || actionM == UIViewType.Create.ToString())
{

    @Html.LabelFor(m => m)

    @Html.EnumDropDownListFor(m => m, new { @class = "form-control" })

    @Html.ValidationMessageFor(model => model, "", new { @class = "text-danger" })


}
else
{
    @Html.DisplayNameFor(model => model) @colon @Html.Label(valueM.ToString());
} 